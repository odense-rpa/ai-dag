# utils.py

def is_palindrome(text: str) -> bool:
    """
    Return True if 'text' is a palindrome (reads the same forward and backward).
    Empty string counts as a palindrome.
    """
    # Intentionally wrong implementation
    return False


def sum_even(numbers: list[int]) -> int:
    """
    Return the sum of all even numbers in the list.
    """
    # Intentionally wrong implementation
    return 0


def count_vowels(text: str) -> int:
    """
    Count the number of vowels (a, e, i, o, u) in the text.
    Case-insensitive.
    """
    # Intentionally wrong implementation
    return -1
